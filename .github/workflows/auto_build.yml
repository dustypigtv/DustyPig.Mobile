name: Auto-Build
on:
  push:
    branches: [ master ]


jobs:
  build:
    name: Build Android
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set default Xamarin SDK versions
        run: $VM_ASSETS/select-xamarin-sdk-v2.sh --mono=6.10 --android=10.2
    
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with: 
          dotnet-version: 6.0.x

      
      - name: Install dependencies
        run: nuget restore DustyPig.Mobile.sln

      - name: Build
        run: msbuild DustyPig.Mobile/DustyPig.Mobile.Android/DustyPig.Mobile.Android.csproj /t:PackageForAndroid /p:Configuration=Debug
