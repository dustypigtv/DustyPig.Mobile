<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:DustyPig.Mobile.Views"
       xmlns:dpcc="clr-namespace:DustyPig"
       Title="Dusty Pig"
       x:Class="DustyPig.Mobile.AppShell">

    <!--
        The overall app visual hierarchy is defined here, along with navigation.
    
        https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/
    -->

    <!-- Make sure Main Rout is first! -->
    <ShellContent Route="StartupPage" ContentTemplate="{DataTemplate local:StartupPage}" />

    <TabBar Route="Main">
        <ShellContent Route="Home" ContentTemplate="{DataTemplate local:HomePage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="FontAwesomeSolid" Glyph="{x:Static dpcc:FontAwesome.House}" />
            </ShellContent.Icon>
        </ShellContent>
        
        <ShellContent Route="Movies" ContentTemplate="{DataTemplate local:MoviesPage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="FontAwesomeSolid" Glyph="{x:Static dpcc:FontAwesome.Film}" />
            </ShellContent.Icon>
        </ShellContent>
        
        <ShellContent Route="Series" ContentTemplate="{DataTemplate local:SeriesPage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="FontAwesomeSolid" Glyph="{x:Static dpcc:FontAwesome.Tv}" />
            </ShellContent.Icon>
        </ShellContent>
        
        <ShellContent Route="Downloads" ContentTemplate="{DataTemplate local:DownloadsPage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="FontAwesomeSolid" Glyph="{x:Static dpcc:FontAwesome.Download}" />
            </ShellContent.Icon>
        </ShellContent>
        
        <ShellContent Route="Settings" ContentTemplate="{DataTemplate local:SettingsPage}">
            <ShellContent.Icon>
                <FontImageSource FontFamily="FontAwesomeSolid" Glyph="{x:Static dpcc:FontAwesome.Gear}" />
            </ShellContent.Icon>
        </ShellContent>
        
    </TabBar>

    
    
</Shell>
